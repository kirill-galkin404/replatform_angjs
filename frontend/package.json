{
  "name": "counter-frontend",
  "version": "0.0.1",
  "description": "counter frontend (React + Vite)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "write-env": "node -e \"require('fs').writeFileSync('dist/env.js', 'window.API_BASE_URL = ' + JSON.stringify(process.env.API_BASE_URL || 'http://localhost:4000') + ';\\n')\"",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.0.1",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
